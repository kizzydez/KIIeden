{
  "name": "contracts",
  "version": "1.0.0",
  "description": "KiiEden NFT + RWA marketplace contracts for KiiChain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network kiiTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network kiiMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.4.2",
    "hardhat": "^2.29.1"
  },
  "private": true,
  "engines": {
    "node": ">=20"
  }
}
